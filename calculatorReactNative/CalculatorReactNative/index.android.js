import { AppRegistry } from "react-native";
import ReactNativeCalculator from "./app/ReactNativeCalculator";

AppRegistry.registerComponent(
  "CalculatorReactNative",
  () => ReactNativeCalculator
);
